# Component Sources (21st MCP research log)

Per `ayesmaj-premium-web` component policy §7. Status "recommended" = research complete, implementation pending authorization.

| Project component | 21st search intent | Selected source/component | Why selected | Dependencies | License/attribution | Major modifications | Status |
|---|---|---|---|---|---|---|---|
| Work archive media grid (~1,500 items) | "filterable masonry media gallery with category filters and lazy loading"; "justified image grid portfolio archive infinite scroll virtualized" | Custom build. Patterns extracted from 21st id 10596 `moumensoliman/gallery-grid-block-shadcnui` (filter a11y contract) and id 2372 `preetsuthar17/responsive-masonry-layout` + id 11979 `easemize/shared-element-gallery` (masonry concept) | All three candidates are demo-scale: per-item framer-motion layout animation, no windowing, no lazy strategy — structurally wrong at 1,000+ items. Extract the filter-button and keyboard contracts; replace CSS columns with deterministic shortest-column distribution from known w/h | none new (framer-motion, lucide-react already installed) | 21st.dev catalog components, paid-tier retrieval; no attribution required; record source ids here | Batch rendering via IntersectionObserver sentinel; `aspect-ratio` reservation from known w/h (no CLS); `content-visibility:auto`; plain CSS tokens; aria-pressed filter group; Enter/Space card activation; reduced-motion gating | Recommended |
| Work archive lightbox (image + video) | "accessible image lightbox with keyboard navigation and video support"; "fullscreen media viewer gallery focus trap escape arrow keys swipe" | 21st id 651 `ibelick/morphing-dialog` (motion-primitives) as a11y skeleton | Only candidate with a real focus trap, focus restore to trigger, Escape, portal, complete dialog ARIA contract; video-in-dialog shown in its own demo | none new (framer-motion already installed; replace `cn`/hook with inline equivalents) | 21st.dev catalog, paid-tier retrieval; no attribution required | Strip per-item provider + `layoutId` morph (one lightbox instance, not 1,500 providers); add ArrowLeft/ArrowRight prev/next over the FILTERED list (fixing the nav-escapes-filter bug seen in id 10596); add drag/swipe-to-dismiss pattern from id 11979; native `<video controls>`; tokens instead of zinc/Tailwind | Recommended |
| /Work editorial hero (layered bg + oversized type + inline chips + 4-6 image cluster) | "editorial portfolio hero with layered background and large display type"; "agency hero with image cluster and category chips"; "cinematic hero with parallax background layers and image cluster" | 21st id 24752 `crafterui/hero-carousel` | Only hero candidate already on `framer-motion` (not `motion/react`), with ZERO npm deps, that threads `useReducedMotion` through every animation (swing, spring, line-wipe, ken-burns). Its layered background is built from the real project photo — hue-grade (`mix-blend-mode: color` + `multiply`) then gradient wash then inline-SVG grain — art direction from the work itself, not glassmorphism. Runner-up 19074 has the right two-column shape but its `ArtCollage` (the exact part needed) is NOT in the retrieved payload, and it wants react-wrap-balancer + CVA + radix-slot + Tailwind v4 `mask-radial-*`. Runner-up 5156 has the 7-image z-layered cluster but runs an infinite float loop with no reduced-motion gate | none new (framer-motion 11 already installed; `cn` inlined) | 21st.dev catalog, paid-tier retrieval; no attribution required | Strip the carousel interaction model: remove the `wheel` listener entirely (a wheel-capturing hero at the top of /Work fights page scroll on the first gesture) and remove `drag`/`dragConstraints`/`onDragEnd`. KEEP click-to-focus, ArrowLeft/Right/Home/End, `aria-current`, `focus-visible` ring. Move the strip from full-bleed centre to a right-hand cluster (4-6 cards, shared top edge, focused card full height, neighbours clipped to half) so oversized Anton type + filter chips own the left column. Replace demo `accent` hues with the brand ramp (#D8B75A / #C58B57 / #A35BDA / #7A48FF) per category. Drop the top bar (AyesmajNav already does that), the mono credit/meta strings and the position rail. Reuse the page's existing `.work-grain` instead of the component's own GRAIN const. Tailwind classes to `work-hero.css` tokens | Recommended |
| /Work featured project spotlight grid (asymmetric, one large + smaller) | "asymmetric featured project grid with one large card"; "bento portfolio showcase with hover reveal"; "editorial case study grid mixed sizes css grid span" | 21st id 2201 `shadcnblockscom/casestudy-5`, with the 12-col span map from id 25300 `uilayout.contact/conditiongrid` | 2201 is the only candidate whose card is a single `<a>` wrapping the whole tile — one tab stop per project, native keyboard activation, no div+onClick — and its "Read case study" affordance is ALWAYS visible, so hover stays pure decoration (satisfies the no-hover-only rule). It is also the only one pinning an explicit `aspect-[14/9]` on the media, which is the mixed-aspect answer. Runner-up 25300 has the better skeleton (real `grid-cols-12` + per-index col-spans) but zero keyboard access — its arrow badge is a bare `div`, no link anywhere — plus `viewport={{once:false}}` re-animating on every scroll pass and a duplicate-key fragment bug. Runner-up 7826 references `bg-black-50`/`border-black-200`, which are not Tailwind classes at all (renders unstyled), and adds 3D tilt with no reduced-motion gate | none new (lucide-react already installed) | 21st.dev catalog, paid-tier retrieval; no attribution required | Take 2201's card contract (whole-tile `<a>`, always-visible CTA row, explicit `aspect-ratio` on media) and drop its flex+border layout for explicit CSS grid: `grid-template-columns: repeat(12,1fr)` with a span map (feature span 7, supporting span 5, then span 6 pairs at >=900px) so one column below 900px is a single `grid-template-columns:1fr` override, not a class permutation. ADD the focus parity 2201 lacks — every `:hover` rule gets a `:focus-visible` twin, since 2201 animates only on `group-hover` and keyboard users see nothing — plus a `prefers-reduced-motion` block killing the media zoom and arrow translate. Strip the dotted radial side panels, `text-primary/50` subtitle tint, `bg-muted/40` hover tint; MoveRight becomes the project's existing gold pill affordance. Tokens: #0D0F0E ground, #141715 surface, #D8B75A accent, Anton titles, DM Sans meta | Recommended |
| /Work filter chip rail (counts + animated active indicator) | "animated filter chips with sliding active indicator"; "segmented control with layout animation and counts" | 21st id 23525 `ddoemonn/filter-grid` (chip rail + `useFilterGrid` hook only) | Best accessibility contract found in this scout: `role="radiogroup"` + `role="radio"` + `aria-checked`, roving tabindex (`tabIndex={on?0:-1}`), Arrow/Home/End nav, `aria-controls` binding the rail to the results region, an `aria-live="polite"` result count, a per-chip `sr-only` "label, N of total", and critically `layoutId={reduced ? undefined : ...}` so the sliding thumb is DISABLED rather than merely faster under reduced motion. It also returns focus to the results region when the focused item is filtered away (`heldFocus`/`settle`). Runner-up 23552 has a smoother mask-based thumb but forces equal-width `repeat(n,1fr)` columns (wrong for 6-8 variable-length labels), carries no counts, and is a fixed `inline-block` with no mobile overflow path. Third: id 22213 `cnippet-dev/v-toggle-10` — shortlisted from metadata only, NOT retrieved, so no code claim is made about it | none new (framer-motion 11 supplies `layoutId`/`useReducedMotion`) | 21st.dev catalog, paid-tier retrieval; no attribution required | Keep ONLY the chip rail + `useFilterGrid` hook; discard the entire `FilterGrid` results half (fixed `rowHeight`/`maxRows` scroll box with `gridAutoRows` is wrong for image cards of mixed aspect). Keep the dual-label opacity crossfade for the active/inactive colour swap. Replace `flex-wrap` with `overflow-x:auto; scroll-snap-type:x proximity` plus an edge fade below 720px so 8 categories scroll horizontally instead of stacking three rows above the fold; `scrollIntoView({block:'nearest',inline:'center'})` on arrow-key move. Tokens: thumb #D8B75A on #07100C text (matches the existing `.rb-filter[data-active]`), rest state `rgba(255,255,255,0.035)` + `1px rgba(255,255,255,0.09)` border, `min-height:44px` (the source's 32px fails the touch target the project already meets), `:focus-visible` to `2px solid #D8B75A` offset 3px, drop the #4568FF focus blue | Recommended |
| /Work discipline spotlight (closing band) | "category showcase band with large image tiles linking to sections, editorial disciplines grid" | 21st id 1960 `youcefbnm/cta-section-with-gallery` | Chosen for one thing: its grid placement. Two columns over five ALTERNATING short/tall tracks (`grid-rows-[50px_150px_50px_150px_50px]`) with each cell spanning two rows at a staggered start, so every tile gets one short + one tall track - identical heights at different vertical offsets. That is an interlock, not a 2x2 contact sheet, and it is the only candidate that pairs a text column with a gallery in one band. Runner-up 25300 `conditiongrid` was already used for the featured span map, and repeating it would have given the page two identical card treatments; 26179 and 26061 are scroll-pinned spectacles that fight a page already 828 items long | none - all of its deps were dropped | 21st.dev catalog, paid-tier retrieval; no attribution required | Its `motion/react` import, `cn` and `Button` deps removed and Tailwind replaced with `work-disciplines.css` tokens. Its staggered blur-in reveal DELIBERATELY NOT KEPT: a `whileInView` reveal starts at `opacity:0`, so a late observer leaves the section blank rather than merely un-animated - measured here, tiles sat at opacity 0 for 3.5s after landing on the band because the archive above keeps the main thread busy decoding lazy media. Content now renders; the only motion left is hover/focus driven, which cannot fail closed (the same reasoning was applied back to `WorkScale`). Its generic "start scaling today" CTA column replaced with a discipline lede - the site footer already carries that exact ask on every page, so the original left /Work with two near-identical closes. The tile IS the link (no hover-synced text list), giving one tab stop and a full-tile tap target; counts and cover art are derived from `WORK_ARCHIVE` at module scope so they cannot drift from the archive. Below 900px the five-track map becomes `grid-template-rows:none` + `grid-auto-rows` - `auto` there still declares one content-sized explicit row, which collapsed the first two tiles to 1px | Shipped |
